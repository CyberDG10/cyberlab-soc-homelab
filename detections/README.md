# Détections

Configurations et règles versionnées :
- `sysmon-config.xml` — configuration Sysmon commentée.
- `wazuh/` — règles de détection personnalisées (local_rules.xml, decoders).

Chaque règle référence la technique MITRE ATT&CK qu'elle couvre.
